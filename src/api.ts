export interface Icategory {
  categoryId: number;
  categoryName: string;
  todoList: [];
}

export interface Iitem {
  todoId: string;
  content: string;
  selectDate: string;
}

export interface Itime {
  timer: [];
  timeWatch: string;
}

export interface Icategory {
  categoryId: number;
  categoryName: string;
  todoList: [];
}
